import 'package:flutter/material.dart';

class GameKidsScreen extends StatelessWidget {
  const GameKidsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: SafeArea(child: Text("Games Kids screen")),
    );
  }
}