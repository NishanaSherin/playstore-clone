import 'package:flutter/material.dart';

class GamePremiumScreen extends StatelessWidget {
  const GamePremiumScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: SafeArea(child: Text("Games Premium screen")),
    );
  }
}