import 'package:flutter/material.dart';

class GameTopScreen extends StatelessWidget {
  const GameTopScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: SafeArea(child: Text("Games Top chart screen")),
    );
  }
}