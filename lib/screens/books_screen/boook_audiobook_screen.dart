import 'package:flutter/material.dart';

class BookAudiobookScreen extends StatelessWidget {
  const BookAudiobookScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return  const Scaffold(
      body: SafeArea(child: Text("This is Audio book Area")),
    );
  }
}