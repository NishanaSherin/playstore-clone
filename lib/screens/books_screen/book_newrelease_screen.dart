import 'package:flutter/material.dart';

class BookNewreleaseScreen extends StatelessWidget {
  const BookNewreleaseScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return  const Scaffold(
      body: SafeArea(child: Text("This is New Release Area")),
    );
  }
}