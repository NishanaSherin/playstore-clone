import 'package:flutter/material.dart';

class BookTopsellScreen extends StatelessWidget {
  const BookTopsellScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return  const Scaffold(
      body: SafeArea(child: Text("This is Top selling Area")),
    );
  }
}